import React from "react";

const Header = () => {
  return (
    <div
      style={{
        textAlign: "center",
        padding: 20,
        color: "#fff",
        background: "#000",
        textTransform: "uppercase",
      }}
    >
      Phone Directory
    </div>
  );
};

export default Header;
